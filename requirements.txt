# Base runtime
opencv-python-headless>=4.10.0
numpy>=1.26.0
requests>=2.32.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0

# Vision inference and tracking (downloads model weights on first start)
ultralytics>=8.3.0

# Optional FRS and ANPR modules; install when the edge image supports them.
# insightface>=0.7.3
# onnxruntime>=1.18.0
# easyocr>=1.7.1
